import React from "react";

export const metadata = {
  title: "This Song | Album Information",
  description: "AI-enhanced analysis of lyrics for songs on Spotify."
};

const layout = ({ children }) => {
  return <>{children}</>;
};

export default layout;
