import React from "react";

export const metadata = {
  title: "This Song | Artist Information",
  description: "AI-enhanced analysis of lyrics for songs on Spotify."
};

const layout = ({ children }) => {
  return <>{children}</>;
};

export default layout;
